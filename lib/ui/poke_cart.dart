import 'package:flutter/material.dart';

import 'components/bottom_nav_bar.dart';

class PokeCart extends StatelessWidget {
  const PokeCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Cart'),
    );
  }
}

