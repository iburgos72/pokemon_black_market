import 'package:flutter/material.dart';

import 'components/bottom_nav_bar.dart';

class PokeWishlist extends StatelessWidget {
  const PokeWishlist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Wishlist'),
    );
  }
}
